// Check if value has a valid value before formatting
if (typeof maintenance_vehicle_km === "undefined" || maintenance_vehicle_km === null || maintenance_vehicle_km === "") { 
    return; 
}  
// Format the value using the Formatter utility
var formattedText = formatAngkaDenganTitik(maintenance_vehicle_km); 
return formattedText;