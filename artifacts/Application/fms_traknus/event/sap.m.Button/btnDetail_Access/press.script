// Get selected row data
const context = oEvent.oSource.getBindingContext();

// Get entire model
const data = context.getObject();

modelFormAccessDetails.setData(data);

FormAccessDetails.setVisible(true);

NavContainer.to(PageAccessDetails);